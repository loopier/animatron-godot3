/fade actor:s

/description Set the actor's opacity realtive to the current value (where 1 is keep as it is). If DUR is set, interpolate the fade over that number of seconds, otherwise change instantaneously.
