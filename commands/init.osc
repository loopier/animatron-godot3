def /startup
    /load/defs midi

def /createsel actor anim
    /create $actor $anim
    /select $actor

def /new actor anim
    /load $anim
    /create $actor $anim

def /newsel actor anim
    /deselect
    /new $actor $anim
    /select $actor

def /help
    /list/commands
